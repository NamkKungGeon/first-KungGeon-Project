a, b, sum = 0, 0, 0

while True :
    a = int(input("첫 번째 수를 입력하세요 : "))
    if a == 0 :
        break
    b = int(input("두 번째 수를 입력하세요 : "))

    sum = a + b

    print("%d + %d = %d입니다." %(a, b, sum))
    print()

print("첫 번째 수에 %d이 입력되서 반복문을 탈출했습니다." %a)