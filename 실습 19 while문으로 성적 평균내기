score, sum, n, avg = 0, 0, 0, 0 #성적, 총합, 입력 갯수, 평균

while score >= 0 :
    score = int(input("성적을 입력하시오 : "))
    if score >= 0 :
        sum = sum + score
        n = n + 1
avg = sum / n

print("성적의 평균은 %f입니다." %avg)
    